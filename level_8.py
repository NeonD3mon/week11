input() # When no prompt is given, the computer will still wait for an answer from the user, without any massage.

Im_a_variable = 5 == 5
input(Im_a_variable) # You can definately use variables as inputs. The program use that as a string for it's prompt.